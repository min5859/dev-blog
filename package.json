{
  "name": "dev-blog",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-site.mjs",
    "check": "npm run build",
    "dev": "npm run build && node scripts/serve-site.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
